import 'package:flutter/material.dart';
import 'package:mobprog_1_tgs/screen.dart';

void main() => runApp(StaticApp());

class StaticApp extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
        home: Screen()
    );
  }
}
